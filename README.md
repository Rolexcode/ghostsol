# GhostSol

A city sighting for $GHOSTSOL on Solana. The short street clip opens the experience; the second film and the original project artwork carry the story through the page.

Run locally with `npm install` and `npm run dev`.
